# Frontend needs the backend WebSocket URL
# For local dev this defaults to ws://localhost:8000 (no need to set)
# For Vercel: set to your public backend URL, e.g.:
#   NEXT_PUBLIC_WS_URL=wss://your-backend.ngrok.io
NEXT_PUBLIC_WS_URL=ws://localhost:8000
